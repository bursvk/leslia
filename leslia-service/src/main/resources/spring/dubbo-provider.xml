<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans.xsd
      http://www.springframework.org/schema/context
      http://www.springframework.org/schema/context/spring-context-4.0.xsd
      http://code.alibabatech.com/schema/dubbo
      http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="leslia-service"/>

    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry  protocol="zookeeper" address="127.0.0.1:2181" />

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880" />


    <dubbo:service interface="com.leslia.inter.api.BookService" ref="bookServiceImpl" timeout="10000"/>

    <dubbo:service interface="com.leslia.inter.api.MessageService" ref="messageServiceImpl" timeout="10000"/>

    <dubbo:service interface="com.leslia.inter.api.InitDemoService" ref="initDemoServiceImpl" timeout="10000"/>

    <dubbo:service interface="com.leslia.user.api.ShiroUserService" ref="shiroUserServiceImpl" timeout="10000"/>

    <dubbo:service interface="com.leslia.inter.api.NumService" ref="numServiceImpl" timeout="10000"/>

    <dubbo:service interface="com.leslia.inter.api.HelpCategoryService" ref="helpCategoryServiceImpl" timeout="10000"/>
    

</beans>